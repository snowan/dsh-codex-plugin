# Evaluation report — 2026-08-17

## Result

The independent adapter passed deterministic protocol tests, real Codex App
Server scenarios, full DSH headless scenarios, and an isolated DSH Web load.

## Versions

| Component | Version |
| --- | --- |
| Codex CLI | `0.147.0` |
| Latest published DSH tested | `0.1.0-rc.6` |
| Source-checkout DSH tested | `0.1.0-rc.5` (`47f943859b`) |
| Node.js requirement | `>=20` |

## Deterministic gates

| Gate | Result |
| --- | --- |
| TypeScript strict typecheck | Pass |
| Unit/contract tests | 12/12 pass |
| JSONL fragmentation and process-close handling | Pass |
| Model pagination and cache | Pass |
| Tool call pause/result/resume on one process | Pass |
| Tool-catalog restart | Pass |
| Image attachment to data URL | Pass |
| npm package dry-run | Pass, 47 files, 30.4 kB compressed |

## Real Codex App Server

`npm run doctor`:

- detected `codex-cli 0.147.0`;
- detected `Logged in using ChatGPT`;
- completed the initialize handshake;
- discovered seven visible models.

Live adapter scenarios with `gpt-5.6-terra`:

| Scenario | Expected | Observed | Time | Result |
| --- | --- | --- | ---: | --- |
| Basic stream | `DSH_CODEX_OK` | `DSH_CODEX_OK` | 6.299s | Pass |
| Dynamic tool | call `fixture_lookup({key:"alpha"})`, then `ALPHA_42` | exact call and output | 6.932s | Pass |

These final live runs used `allowCodexNativeTools: false`; the dynamic tool
continued to work with Codex-native action features disabled.

## Full DSH runs

| DSH | Scenario | Evidence | Result |
| --- | --- | --- | --- |
| `0.1.0-rc.5` | Exact answer | durable response from `codex-cli / gpt-5.6-terra` | Pass |
| `0.1.0-rc.5` | Real DSH `read` tool | session log contains `tool/call name=read` and matching result | Pass |
| `0.1.0-rc.6` | Real DSH `read` tool | returned `@snowan/dsh-codex-plugin`; trajectory identifies Codex provider/model; native Codex tools disabled | Pass |

The latest-release scenario used an isolated profile under `/private/tmp`,
installed this checkout as a DSH bundle, and ran through the official headless
surface. No DeepSeek API key was present or required.

## Direct Codex comparison

Both paths used `gpt-5.6-terra` and the same workspace. Times are single-run
wall-clock smoke measurements, not a benchmark.

| Scenario | DSH + plugin | Direct `codex exec` | Functional result |
| --- | ---: | ---: | --- |
| Exact answer | 5.785s | 6.37s | both exact |
| Read root package name | 11.703s | 11.24s | both `@deepseek-ai/dsh-root` |

The DSH path used DSH's `read` tool and durable agent loop. Direct Codex used
its native command execution. Prompt context, cache state, and tool surfaces
differ, so the timing delta should not be generalized. The useful conclusion
is functional parity with modest adapter overhead in this sample.

## DSH Web

An isolated Web profile served successfully on `127.0.0.1:3081`. The Web plugin
inventory showed `codex-plugin` as `Mounted, Enabled` at loader row
`include:llm-codex-cli`. The composed config resolved the default to
`codex-cli / gpt-5.6-terra`.

The isolated profile had no saved workspace. Its native directory picker was
not automated, so the browser chat input remained disabled. End-to-end chat
was instead verified through DSH headless, which uses the same agent and LLM
loop without the workspace-picker UI dependency.

## Known limits and next gates

- Repeat the comparison across multiple trials before making latency claims.
- Add Windows and Linux CI; current live evidence is macOS arm64.
- Add a DSH Web browser chat fixture with a pre-seeded workspace.
- Re-run schema generation and contract tests for every Codex CLI upgrade.
